name: back side

on:
  workflow_dispatch:

jobs:
  print-numbers:
    runs-on: ubuntu-latest

    steps:
      - name: Print Numbers 1 to 10
        run: |
          for i in {10...1}
          do
            echo "N: $i"
          done
