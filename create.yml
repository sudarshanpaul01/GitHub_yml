name: Print 10 Numbers

on:
  workflow_dispatch:

jobs:
  print-numbers:
    runs-on: ubuntu-latest

    steps:
      - name: Print Numbers 1 to 10
        run: |
          for i in {1..10}
          do
            echo "Number: $i"
          done
